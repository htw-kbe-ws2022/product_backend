package com.htwkbews2022.product_backend.core.services;

class ProductServiceHelperTest {
  /*  static ProductRepoInterface mockProductRepo = mock(ProductRepoInterface.class);
    static Product product = new Product();

    @BeforeAll
    static void setup(){
        UUID uuid = UUID.randomUUID();
        product.setId(uuid);
        product.setTitle("hanfÖl");
        product.setCategory(Category.PROCESSED);
        product.setCount(5);
        product.setPrice(3.99);
        product.setCurrency(Currency.EURO);
        product.setDescription("Very good homade stuff or oil");
        product.setUrl("http://www.du_doch_auch.de");
        mockProductRepo.save(product);
    }

    @Test
    void changeCount() {
        when(mockProductRepo.getReferenceById(any())).thenReturn(product);
        ProductService productService = new ProductService();
        var newCount = productService.changeCount(product.getId(), 5);
        assertEquals(10, newCount);
    }

    @Test
    void changeProductDetail() {
   } */
}